{
  "name": "shelfbot-proxy",
  "version": "0.1.0",
  "description": "Auth + OpenAI proxy for ShelfBot. Keeps the OpenAI key off user devices, enforces a daily per-user query cap, and provides the identity layer that paid plans will plug into.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev":   "node --watch src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "cors":           "^2.8.5",
    "dotenv":         "^16.4.5",
    "express":        "^4.21.1",
    "jsonwebtoken":   "^9.0.2"
  }
}
